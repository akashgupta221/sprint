{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node -e \"const fs = require('fs'); ['package-lock.json','yarn.lock'].forEach(f => { try { fs.unlinkSync(f); } catch {} }); if (!process.env.npm_config_user_agent?.startsWith('pnpm/')) { console.error('Use pnpm instead'); process.exit(1); }\"",
    "dev": "pnpm --filter @workspace/api-server run build && pnpm --parallel --filter @workspace/api-server --filter @workspace/agile-pm --if-present run dev",
    "dev:api": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/agile-pm run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "lightningcss": "^1.32.0",
    "lightningcss-darwin-arm64": "^1.32.0"
  }
}
